import type { ModelReq, ImageGenerateInput, ImageEditInput, HunyuanGenerateImageInput, HunyuanGenerateImageOutput, HunyuanEditImageInput, HunyuanEditImageOutput } from '../type';
type NoInfer<T> = [T][T extends any ? 0 : never];
type GenerateImageInput<TProvider extends string, TInput = unknown> = unknown extends TInput ? TProvider extends 'hunyuan-exp' ? HunyuanGenerateImageInput : ImageGenerateInput : TInput;
type GenerateImageOutput<TProvider extends string, TOutput = unknown> = unknown extends TOutput ? TProvider extends 'hunyuan-exp' ? HunyuanGenerateImageOutput : unknown : TOutput;
type EditImageInput<T extends string, TInput = unknown> = unknown extends TInput ? T extends 'hunyuan-exp' ? HunyuanEditImageInput : ImageEditInput : TInput;
type EditImageOutput<T extends string, TOutput = unknown> = unknown extends TOutput ? T extends 'hunyuan-exp' ? HunyuanEditImageOutput : unknown : TOutput;
export declare class DefaultImageModel<const TProvider extends string> {
    private req;
    baseUrl: string;
    provider: TProvider;
    generateImageSubUrl: string;
    editImageSubUrl: string;
    constructor(req: ModelReq, baseUrl: string, provider: TProvider);
    generateImage<TInput = unknown, TOutput = unknown>(input: NoInfer<GenerateImageInput<TProvider, TInput>>): Promise<GenerateImageOutput<TProvider, TOutput>>;
    editImage<TInput = unknown, TOutput = unknown>(input: NoInfer<EditImageInput<TProvider, TInput>>): Promise<EditImageOutput<TProvider, TOutput>>;
    private getGenerateUrl;
    private getEditUrl;
}
export {};
